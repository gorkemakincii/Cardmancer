# Deployed backend URL (the Render service). Used by the Vite build for both
# the REST API and the Socket.io connection.
# Local development: leave this unset — it defaults to http://localhost:3001.
VITE_SERVER_URL=https://power-hungry-pets-server.onrender.com
